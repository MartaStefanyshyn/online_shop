<h1>Products</h1>
<% @category.products.in_groups_of(3) do |group| %>
  <div class="row">
    <% group.compact.each do |product| %>
      <div class="col-md-4">
        <%= image_tag product.photo.url(:medium)%>
        <h4><%= link_to product.name, product %></h4>
        <p>Price: <%= number_to_currency(product.price) %></p>
        <%= button_to 'Add to Order', order_products_path(product_id: product), class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
<% end %>
<br>
<br>
<%= link_to categories_path, class: 'btn btn-info btn-xs' do%>
 <i class="glyphicon glyphicon-menu-left"></i>Back
 <% end %>



